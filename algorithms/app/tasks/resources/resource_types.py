from enum import Enum

class ResourceType(Enum):
    APK_FILE = 0,
    SCREENSHOT = 1,
    XML_LAYOUT = 2,
    JSON_LAYOUT = 3,
    ACCESSABILITY_ISSUE = 4,
    DISPLAY_ISSUE = 5,
    TAPPABILITY_PREDICT = 6,
    UTG = 7,
    GIF = 8,
    ZIPS = 9,
    EMULATOR = 10,

